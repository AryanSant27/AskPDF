see the app.py it is a rag based application. what we need to do is create a backend in flask, frontend in HTML CSS and JS, integrate a cloud vector databse, integrate a user authentication, integrate a DB. Now first make a goals.txt which lists all the goals. Then make a progress.txt which is a progress log that will keep track of the progress. read the app.py and after that delete it becasue we want it to start from zero...also try to avoid the langchain package because it is nowhere near production ready. for the LLM we can use gemini 1.5 flash and for databse we can use MongoDB. Also we need two more features. 1. The vector embeddings to be stored in cache so that the person when talking to same pdf won't have to chunk it again. 2. we need to implement 'chatting' feature where we can actually continue and the LLM will have a context of PDF annddd the Chat.     